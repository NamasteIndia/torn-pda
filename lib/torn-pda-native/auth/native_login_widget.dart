// Stub implementation for CI builds
// This file is gitignored - developers should create their own version

import 'package:flutter/material.dart';

class NativeLoginWidget extends StatelessWidget {
  const NativeLoginWidget({super.key});

  @override
  Widget build(BuildContext context) {
    // Stub implementation - returns empty container
    return const SizedBox.shrink();
  }
}
